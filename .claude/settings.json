{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && pnpm exec biome check --write --files-ignore-unknown=true \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Running pnpm fix"
          }
        ]
      }
    ]
  }
}
